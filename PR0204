#include <stdio.h>
#include <stdlib.h>

int main()
{
    float a,b,c,d;
    float mean;

    scanf("%f %f %f %f", &a, &b, &c, &d);

    mean = (a+b+c+d)/4;
    //printf("%d", mean);

    if (a >= mean)
    {
        printf("SUCCESS");
    }
    else
    {
        printf("FAILED");
    }

    return 0;
}
