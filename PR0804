#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char test[10000];
    int string_length;
    int i;

    scanf("%s", test);
    string_length = strlen(test);

    for(i= string_length - 1; i >= 0; i--)
    {
        printf("%c", test[i]);
    }

    return 0;
}
