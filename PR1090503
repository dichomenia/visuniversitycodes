#include <iostream>

using namespace std;


int main()
{
    int num,count=0;

    cin >> num;

    while(num!=0)
    {
        count=0;
        for(int i = 1;i <= num; i++)
        {
            if(num%i == 0)
            {
                count++;
            }
        }

    if(count == 2)
    {
        cout << "prime"<<endl;
    }
    else
    {
        cout << "not a prime"<<endl;
    }

    cin >> num;

    }
    return 0;
}
