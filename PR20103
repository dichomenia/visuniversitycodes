#include <iostream>

using namespace std;

int main()
{
    long int a,b,d;    //able to hold 2^32
    while(cin>>a>>b)
    {
        d=(a-b);
        if (d < 0)
        {
            d = d * -1;
        }
        cout<<d<<endl;
    }
    return 0;
}
